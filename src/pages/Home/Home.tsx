import { PageContainer, SideBar, ContentContainer } from "@/components";

function Home() {
  return (
    <PageContainer>
      <SideBar />
      <ContentContainer />
    </PageContainer>
  );
}

export { Home };
